---
tags:
  - DB
---

# 学習データセット回復手順

```sql
CREATE TABLE IF NOT EXISTS systems (
    id          INTEGER PRIMARY KEY,
    name        VARCHAR(128) NOT NULL
);

CREATE TABLE IF NOT EXISTS readings (
    system_id   INTEGER NOT NULL,
    read_on     TIMESTAMP NOT NULL,
    power       DECIMAL(10,3) NOT NULL
            DEFAULT 0 CHECK(power >= 0), -- デフォルト値: 0 CHECKにて0以上
    PRIMARY KEY (system_id, read_on),
    FOREIGN KEY (system_id)
            REFERENCES systems(id)
);

INSERT INTO systems(id, name)
         SELECT DISTINCT system_id, system_public_name
         FROM 'https://oedi-data-lake.s3.amazonaws.com/pvdaq/csv/systems.csv'
         ORDER BY system_id ASC;

INSERT INTO readings(system_id, read_on, power)
         SELECT system_id, measured_on,
             CASE
                 WHEN ac_power_hw__2695 < 0 OR ac_power_hw__2695 IS NULL THEN 0
                 ELSE ac_power_hw__2695 END AS ac_power
         FROM read_csv_auto(
           's3://oedi-data-lake/pvdaq/csv/pvdata/system_id=34/year=2019/*/*/*.csv',
           union_by_name=True);

CREATE OR REPLACE VIEW v_power_per_day AS
SELECT system_id,
    date_trunc('day', read_on) AS day,
    round(sum(power) / 4 / 1000, 2) AS kWh,
FROM readings
GROUP BY system_id, day;

SELECT count(system_id) FROM readings;
┌──────────────────┐
│ count(system_id) │
│      int64       │
├──────────────────┤
│            46041 │
└──────────────────┘
```

# 高度な主役とデータ分析

## 取り込み時の事前集計

```
-- ファイル2020_10.csvの末尾3行を取得
$ duckdb -s ".maxwidth 40" -s "FROM read_csv_auto('2020_10.csv') LIMIT 3"
┌────────┬─────────────────────┬───┬───────────────┬────────────────┐
│ SiteID │      Date-Time      │ … │ module_temp_3 │ poa_irradiance │
│ int64  │      timestamp      │ … │    double     │     double     │
├────────┼─────────────────────┼───┼───────────────┼────────────────┤
│     10 │ 2020-01-23 11:20:00 │ … │        14.971 │         748.36 │
│     10 │ 2020-01-23 11:21:00 │ … │        14.921 │         638.23 │
│     10 │ 2020-01-23 11:22:00 │ … │        14.895 │         467.67 │
└────────┴─────────────────────┴───┴───────────────┴────────────────┘

$ duckdb -s ".maxwidth 40" -s "FROM read_csv_auto('2020_1200.csv') LIMIT 3"
┌────────┬─────────────────────┬───┬──────────────────┬──────────────┐
│ SiteID │      Date-Time      │ … │ ac_power_metered │ power_factor │
│ int64  │      timestamp      │ … │      int64       │    double    │
├────────┼─────────────────────┼───┼──────────────────┼──────────────┤
│   1200 │ 2020-01-01 00:00:00 │ … │               20 │        0.029 │
│   1200 │ 2020-01-01 00:05:00 │ … │               20 │        0.029 │
│   1200 │ 2020-01-01 00:10:00 │ … │               20 │        0.029 │
└────────┴─────────────────────┴───┴──────────────────┴──────────────┘
-- tailコマンドでは完全な代替はできない
$ duckdb -s ".maxwidth 40" -s "FROM read_csv_auto('2020_1200.csv')" | tail -n 3
│   1200 │ 2020-06-26 11:00:00 │ … │            38040 │       -0.988 │
└────────┴─────────────────────┴───┴──────────────────┴──────────────┘
  51101 rows (40 shown)                          6 columns (4 shown)
```

`time_bucket()`
: タイムスタンプを指定した間隔に切り捨て、任意のオフセットに整列させる。

```sql
INSERT INTO readings(system_id, read_on, power)
SELECT any_value(SiteId),
  time_bucket(
    INTERVAL '15 Minutes',
    CAST("Date-Time" AS timestamp)
  ) AS read_on,
avg(
  CASE
    WHEN ac_power < 0 OR ac_power IS NULL THEN 0
    ELSE ac_power END)
FROM
  read_csv_auto(
    's3://oedi-data-lake/pvdaq/csv/pvdata/system_id=34/year=2019/*/*/*.csv'
  )
GROUP BY read_on
ORDER BY read_on;

-- レコードレイアウトがかわったため動作せず
Binder Error:
Referenced column "Date-Time" not found in FROM clause!
Candidate bindings: "system_id", "ambient_temp_f__2688", "relative_humidity__2687", "ac_voltage__2684"
```

## 要約する

`SUMMARIZE` でデータセットを要約できる。

```sql
SUMMARIZE readings;
┌─────────────┬───────────────┬───┬───────┬─────────────────┐
│ column_name │  column_type  │ … │ count │ null_percentage │
│   varchar   │    varchar    │ … │ int64 │  decimal(9,2)   │
├─────────────┼───────────────┼───┼───────┼─────────────────┤
│ system_id   │ INTEGER       │ … │ 46035 │            0.00 │
│ read_on     │ TIMESTAMP     │ … │ 46035 │            0.00 │
│ power       │ DECIMAL(10,3) │ … │ 46035 │            0.00 │
└─────────────┴───────────────┴───┴───────┴─────────────────┘
```

```sql
.mode line -- lineモードに切り替え
-- 測定値の一部を要約
SUMMARIZE SELECT read_on, power FROM readings WHERE system_id = 1200;
    column_name = read_on
    column_type = TIMESTAMP
            min = NULL
            max = NULL
  approx_unique = 0
...
          count = 0
null_percentage = NULL

.mode duckbox -- 通常モードに復帰
```

## サブクエリ

```sql
-- 非相関サブクエリ
-- 生産した合計電力の平均
-- sum()/avg() を利用
SELECT avg(sum_per_system)
FROM (
  SELECT sum(kWh) AS sum_per_system
  FROM v_power_per_day
  GROUP BY system_id
);
┌─────────────────────┐
│ avg(sum_per_system) │
│       double        │
├─────────────────────┤
│  102871.73999999998 │
└─────────────────────┘
```

```sql
-- スカラ非相関サブクエリ
-- どの日にどのシステムで最も多くの電力を生産したのか
SELECT read_on, power
FROM readings
WHERE power = (
  SELECT max(power) FROM readings
);
┌─────────────────────┬───────────────┐
│       read_on       │     power     │
│      timestamp      │ decimal(10,3) │
├─────────────────────┼───────────────┤
│ 2019-05-23 11:00:00 │    133900.000 │
│ 2019-05-23 12:30:00 │    133900.000 │
│ 2019-05-28 12:45:00 │    133900.000 │
│ 2019-05-08 13:15:00 │    133900.000 │
└─────────────────────┴───────────────┘
```

```sql
-- スカラ相関サブクエリ
SELECT system_id, read_on, power
FROM readings r1
WHERE power = (
  SELECT max(power)
  FROM readings r2
  -- 外側クエリと内側サブクエリを結合
  WHERE r2.system_id = r1.system_id
)
ORDER BY ALL;
┌───────────┬─────────────────────┬───────────────┐
│ system_id │       read_on       │     power     │
│   int32   │      timestamp      │ decimal(10,3) │
├───────────┼─────────────────────┼───────────────┤
│        10 │ 2023-06-05 13:00:00 │      3500.000 │
│        34 │ 2019-05-08 13:15:00 │    133900.000 │
│        34 │ 2019-05-23 11:00:00 │    133900.000 │
│        34 │ 2019-05-23 12:30:00 │    133900.000 │
│        34 │ 2019-05-28 12:45:00 │    133900.000 │
└───────────┴─────────────────────┴───────────────┘
```

外部テーブルとの非相関サブクエリ結合

```sql
SELECT r1.system_id, read_on, power
FROM readings r1
JOIN (
  SELECT r2.system_id, max(power) AS value
  FROM readings r2
  GROUP BY ALL
) AS max_power ON (
  max_power.system_id = r1.system_id AND
  max_power.value = r1.power
)
ORDER BY ALL;
┌───────────┬─────────────────────┬───────────────┐
│ system_id │       read_on       │     power     │
│   int32   │      timestamp      │ decimal(10,3) │
├───────────┼─────────────────────┼───────────────┤
│        10 │ 2023-06-05 13:00:00 │      3500.000 │
│        34 │ 2019-05-08 13:15:00 │    133900.000 │
│        34 │ 2019-05-23 11:00:00 │    133900.000 │
│        34 │ 2019-05-23 12:30:00 │    133900.000 │
│        34 │ 2019-05-28 12:45:00 │    133900.000 │
└───────────┴─────────────────────┴───────────────┘
```

### 式としてのサブクエリ

式としてのサブクエリは、常に1行だけ返す必要がある。

`EXISTS` 別のテーブルの1行に存在する値を持つ行をすべて選択する。

```sql
.mode line
-- vという名前の列を1つ持つインラインテーブル sを定義
SELECT * FROM VALUES (7), (11) s(v)
WHERE EXISTS (SELECT * FROM range(10) WHERE range = v);
v = 7
```

`IN` 少なくとも1回含まれている場合、真と評価される。

```sql
.mode line
SELECT * FROM VALUES (7), (11) s(v)
WHERE v IN (SELECT * FROM range(10));
```

`ANY` 外側の値と内側の値いずれかとの比較が真として評価される。

```sql
.mode line
SELECT * FROM VALUES (7), (11) s(v)
WHERE v = ANY (SELECT * FROM range(10));
```

`ALL` 外側の値と内側の値との比較がすべて真として評価される。

```sql
.mode line
SELECT * FROM VALUES (7), (11) s(v)
WHERE v = ALL (SELECT 7);
```

## グループ化セット

様々な集約を使う。

```sql
.mode duckbox
SELECT count(*),
  min(power) AS min_W, max(power) AS max_W,
  -- 15分毎のW単位からkWhに変換
  round(sum(power) / 4 / 1000, 2) AS kWh
FROM readings;
```

```sql
-- kWhでシステム毎の発電量を求める
SELECT
  year(read_on) AS year,
  system_id, count(*),
  round(sum(power) / 4 / 1000, 2) AS kWh
FROM readings
GROUP BY year, system_id
ORDER BY year, system_id;
```

`GROUPING SETS`

```sql
-- 測定値と総発電量それぞれの
-- 年毎、年とシステム毎、全体の値を求める
SELECT
  year(read_on) AS year,
  system_id, count(*),
  round(sum(power) / 4 / 1000, 2) AS kWh
FROM readings
GROUP BY GROUPING SETS ((year, system_id), year, ())
ORDER BY year NULLS FIRST, system_id NULLS FIRST;
```

`GROUP BY GROUPING SETS ((year, system_id), year, ())` の意味

- `(year, system_id)` `year` \* `system_id` の組み合わせパケット
- `year` のみ、このパケットに含まれないキーはNULLをあてる
- `()` 空のパケットまたは空のグループ

`GROUP BY ROLLUP` を使って書き換える。

```sql
SELECT
  year(read_on) AS year,
  system_id, count(*),
  round(sum(power) / 4 / 1000, 2) AS kWh
FROM readings
GROUP BY ROLLUP (year, system_id)
ORDER BY year NULLS FIRST, system_id NULLS FIRST;
```

`GROUP BY CUBE`

```sql
-- GROUP BY CUBE を使ってシステム毎の合計を求める
SELECT
  year(read_on) AS year,
  system_id, count(*),
  round(sum(power) / 4 / 1000, 2) AS kWh
FROM readings
GROUP BY CUBE (year, system_id)
ORDER BY year NULLS FIRST, system_id NULLS FIRST;
```

## ウィンドウ関数

- 他の行を見ることができるようになる
- `GROUP BY` のように集約されない
- `OVER` 句を用いて宣言する

```sql
-- NG: 最も多くの電力が生産された上位3つの時刻
SELECT * FROM readings ORDER BY power DESC LIMIT 3;
```

```sql
-- OK: 最も多くの電力が生産された上位3つの時刻
WITH ranked_readings AS (
  SELECT *,
    dense_rank()
    -- ウィンドウは発電量の多い順で1行毎に開かれる
    OVER (ORDER BY power DESC) AS rnk
  FROM readings
)
SELECT *
FROM ranked_readings
WHERE rnk <= 3;
```

### パーティションの定義

`PARTITION BY` `COLUMN_NAME` を引数に与えてウィンドウとして機能する。

```sql
-- システム毎の消費電力量上位2位
WITH ranked_readings AS (
  SELECT *,
  dense_rank()
  OVER (-- 移動するウィンドウの定義開始
    PARTITION BY system_id -- パーティション定義、ウィンドウの自体
    ORDER BY power DESC
  ) AS rnk
  FROM readings
)
SELECT * FROM ranked_readings WHERE rnk <= 2
ORDER BY system_id, rnk ASC;
```

```sql
-- システム毎の毎日の発電量と
-- システム全体の平均発電量
SELECT *,
  avg(kWh)
    OVER (
      PARTITION BY system_id
    ) AS average_per_system
FROM v_power_per_day LIMIT 3;
```

### フレーミング

関数が評価される各行から見た相対的な行集合を指定する。現在の行からの距離は、行の前/行の後に指定された式として与えられる。

```sql
SELECT system_id, day, kWh, avg(kWh)
  OVER (
    PARTITION BY system_id -- system_id毎にパーティション上を移動
    ORDER BY day ASC -- 日付昇順
    RANGE BETWEEN INTERVAL 3 Days PRECEDING  - - 前に3日間,合計7日間
      AND INTERVAL 3 Days FOLLOWING -- 後に3日間
  ) AS "kWh 7-day moving average"
FROM v_power_per_day
ORDER BY system_id, day;
```

### 名前付きウィンドウ

```sql
SELECT system_id,
  day,
  min(kWh) OVER seven_days AS "7-day min", -- 定義されたウィンドウをFROM句で参照
  quantile(kWh, [0.25, 0.5, 0.75]) -- 分位数を計算
    OVER seven_days AS "kWh 7-day quantile",
  max(kWh) OVER seven_days AS "7-day max",
FROM v_power_per_day
WINDOW -- FROM語句の後で宣言
  seven_days AS (
    PARTITION BY system_id, month(day)
    ORDER BY day ASC
    RANGE BETWEEN INTERVAL 3 Days PRECEDING
      AND INTERVAL 3 Days FOLLOWING
  )
ORDER BY system_id, day;
```
